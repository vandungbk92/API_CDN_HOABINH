version: "3.2"
services:
  bvhoabinhfile:
    image: "registry.thinklabs.com.vn:5000/bvhoabinhfile:latest"
    deploy:
      replicas: 1
      placement:
        constraints: [node.labels.environment==development]
      restart_policy:
        condition: any
    environment:
      PORT: 3001
      NODE_ENV: "production"
      SERVICE_3001_NAME: "bvhoabinhfile"
      SERVICE_NAME: "bvhoabinhfile"
      SERVICE_TAGS: "bvhoabinhfile"
    ports:
      - target: 3001
        published: 5000
        mode: host
    volumes:
      - uploads:/usr/src/app/uploads

volumes:
  uploads:
    driver: lizardfs
