version: "3.2"
services:
  cdn:
    image: "registry.thinklabs.com.vn:5000/bvbinhdinhcdn:latest"
    deploy:
      replicas: 1
      placement:
        constraints: [node.labels.environment==development]
      restart_policy:
        condition: any
    environment:
      PORT: 3001
      NODE_ENV: "production"
      SERVICE_3001_NAME: "bvbinhdinhcdn"
      SERVICE_NAME: "bvbinhdinhcdn"
      SERVICE_TAGS: "bvbinhdinhcdn"
    ports:
      - target: 3001
        published: 4035
        mode: host
    volumes:
      - uploads:/usr/src/app/uploads

volumes:
  uploads:
    driver: lizardfs
