version: "3.2"
services:
  cdn:
    image: "registry.thinklabs.com.vn:5000/bvtrieusoncdn:latest"
    deploy:
      replicas: 1
      placement:
        constraints: [node.labels.environment==development]
      restart_policy:
        condition: any
    environment:
      PORT: 3001
      NODE_ENV: "production"
      SERVICE_3001_NAME: "bvtrieusoncdn"
      SERVICE_NAME: "bvtrieusoncdn"
      SERVICE_TAGS: "bvtrieusoncdn"
    ports:
      - target: 3001
        published: 4022
        mode: host
    volumes:
      - uploads:/usr/src/app/uploads

volumes:
  uploads:
    driver: lizardfs
