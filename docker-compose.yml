version: "3.2"
services:
  cdn:
    image: "registry.thinklabs.com.vn:5000/bvducgiangcdn:latest"
    deploy:
      replicas: 1
      placement:
        constraints: [node.labels.environment==development]
      restart_policy:
        condition: any
    environment:
      PORT: 3001
      NODE_ENV: "production"
      SERVICE_3001_NAME: "bvducgiangcdn"
      SERVICE_NAME: "bvducgiangcdn"
      SERVICE_TAGS: "bvducgiangcdn"
    ports:
      - target: 3001
        published: 4037
        mode: host
    volumes:
      - uploads:/usr/src/app/uploads

volumes:
  uploads:
    driver: lizardfs
